# change the variables below and the name to .auto.tfvars

gcp_project = "gcp-project-id"

prefix = "cka-training"

region = "gcp-region"
zone   = "gcp-zone"

ssh_pub_key_file_path = "path-to-local-private-key"
